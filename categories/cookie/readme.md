Cookie monster recipe

-Try to login first with any credentials to create a trigger the cookie

-Cookies are small pieces of data a website stores in your browser to remember things eg

session IDs
preferences eg theme
analytics data

-Based on the hints the value of the cookie was the flag,,but in base 64 encoding,so we decode

-I used the browser cosole and atob and flag was displayed
